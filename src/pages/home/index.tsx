import { Loading } from "~/components/loading";

export const Index = () => {
    return (
        <div className="w-full h-full flex justify-center items-center">
            <Loading />
        </div>
    );
};
